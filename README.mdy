# 自定义串口工具( smartCom)
## 需求:
### 1.能够支持多种串口驱动器,如CH340,CP2102.
### 2.串口支持自定义应用层协议
	2.1 应用层协议能够用C语言自定义校验算法.
	2.2 协议能够自定义分层,例如HEAD+LEN+DATA+CAL 的形式,亦或者是 HEAD_PACKET + LEN + DATA + CAL 的形式, 亦或者是整个数据报文都是加密的,需要解密之后再转化成前两种形式,也可以是HEAD + LEN + CMD + SEQ + DATA + CAL.
	    也就是说用户能够自定义自己的协议是如何组成的,可以随意的拆解报文.只要接收到的数据能够按用户给定的方式解析出来,就认为这是一条符合用户协议的报文.
	2.3 用户可以单独的拥有一个协议解析窗口,解析协议的数据,然后按用户的喜好显示数据,比如报文分 HEAD +DEV_ID + LEN + CMD_ID + DATA + CAL 用户可以选择展示 DEV_ID, CMD_ID, DATA. 然后解析出来的协议数据只展示对应的数据.
	2.4 用户可以对解析出来的协议数据做再处理,比如,每条CMD_ID = 0X15 的数据都要显示红色的背景色,或者是对DATA 数据做再处理(例如DATA 是一个结构体,数据组成为LEN + STRING) 当然这个预处理是可以选择绑定某个数据段,如DEV_ID或是CMD_ID,符合之后才会这么处理.
	    用户也可以筛选某个数据段为指定值的数据,例如CMD_ID = 0x15 或者是0x18。
	2.5 默认的协议数据解析时间为500MS,用户可以自定义这个解析时间,这个解析时间代表了: 最后一次接受到数据,假如其不符合协议格式,等待多长时间认为不存在协议数据,抛弃该缓存.
	2.6 为了快速解析出协议数据,串口工具可以分段识别数据,将不符合的直接丢弃,例如用户定义了协议需要先发送一个0xFF的头,随后开始解析数据,那么串口工具将在接收到0xFF之后开始协议解析,亦或者用户定义需要先接受到一个头的报文帧,组成为HEAD + DATA_LEN + DEV + SEQ + HEAD_CAL（ 用户可以随意定义,他甚至能定义两个头),
	    那么就在解析出符合这个报文帧数据的数据后开始协议解析,其余的都不视为协议数据.
### 3 正则表达式,串口工具能够通过符号正则表达式来区分哪些串口数据需要被展示,哪些串口数据不需要被展示.
	3.1 用户可以选中串口中的数据,然后通过鼠标右键,选择需要正则筛选展示这些数据还是不展示这些数据.
	3.2 这种数据的正则筛选以\n 为结束.
### 4 串口展示波形,用户可以定义曲线窗口,数据的个数为X轴,Y轴展示用户通过正则/协议解析 筛选出来的数据,例如形如 x:0xHH y:0xHH z:0xHH 的数据,用户可以展示出x,y,z 的数据曲线, CMD_ID = 0x15 的DATA,用户可以选择用来做曲线展示.
### 5 多窗口,用户可以拥有多个串口数据展示的串口,例如同时能够展示原窗口(不对数据做处理的窗口,协议层窗口,正则窗口,波形窗口).
### 6 配置保存,用户可以保存自己的配置,在其他电脑上导入生效.
### 7 缓冲区,为了避免过度占用用户的内存,工具运行时在自己安装的文件夹中生成缓存文件,每次满了就清空,文件的大小处定义是1MB, 用户可以自定义保存全部数据,会让用户新建一些缓存文件(存储波形,原窗口数据,正则过滤的数据等),这种创建出来的文件不限制大小.
### 8.其余通用串口工具的功能,例如设置波特率,设置硬件层的解析格式,如奇校验偶校验等,显示发送接受的数量,选择使用哪个硬件驱动,关闭打开,同时显示发送,接受,智能分包等等.
### 9.多平台,可以在MAC,LINUX,windows 上使用.

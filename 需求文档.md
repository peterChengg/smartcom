# smartCom 自定义串口工具需求文档

## 项目概述

smartCom 是一款功能强大的自定义串口通信工具，专为需要灵活协议解析和数据分析的用户设计。该工具支持多种串口驱动器，提供可配置的应用层协议解析、数据过滤、波形可视化等高级功能。

## 核心功能需求

### 1. 多驱动器支持
- **支持的驱动器**: CH340、CP2102 等主流串口驱动器
- **统一接口**: 为不同驱动器提供统一的操作接口
- **自动检测**: 自动识别并加载可用的串口驱动器

### 2. 自定义应用层协议

#### 2.1 协议结构自定义
- **灵活的协议格式**: 支持用户自定义协议分层结构
- **常见协议格式**:
  - `HEAD + LEN + DATA + CAL`
  - `HEAD_PACKET + LEN + DATA + CAL`
  - `HEAD + LEN + CMD + SEQ + DATA + CAL`
- **加密协议支持**: 支持加密数据报文的解密后解析
- **报文拆解**: 用户可随意定义报文组成方式和解析规则

#### 2.2 协议解析窗口
- **专用解析界面**: 提供独立的协议数据解析窗口
- **字段选择性显示**: 用户可选择显示特定字段（如 DEV_ID、CMD_ID、DATA）
- **实时解析**: 实时解析并显示协议数据

#### 2.3 数据后处理
- **条件格式化**: 基于数据段值进行条件格式化（如 CMD_ID=0x15 显示红色背景）
- **数据结构解析**: 支持对 DATA 段进行结构化解析（如 LEN + STRING 格式）
- **绑定条件**: 后处理规则可绑定到特定数据段（DEV_ID、CMD_ID 等）
- **数据筛选**: 支持筛选特定值的数据段

#### 2.4 解析时间配置
- **默认解析时间**: 500ms
- **用户自定义**: 允许用户调整协议解析超时时间
- **缓存管理**: 超时后自动清理无效缓存数据

#### 2.5 分段识别机制
- **头标识识别**: 支持基于特定字节头（如 0xFF）的数据识别
- **帧头识别**: 支持复杂帧头结构识别（如 HEAD + DATA_LEN + DEV + SEQ + HEAD_CAL）
- **多级头支持**: 支持定义多个头的识别机制
- **数据过滤**: 自动丢弃不符合协议格式的数据

### 3. 正则表达式数据过滤

#### 3.1 交互式过滤
- **右键菜单**: 用户可选中数据并通过右键菜单设置过滤规则
- **过滤模式**: 支持包含模式（展示匹配数据）和排除模式（隐藏匹配数据）
- **实时预览**: 实时预览过滤效果

#### 3.2 过滤规则
- **行级过滤**: 以换行符 `\n` 为单位的过滤
- **正则表达式**: 支持完整的正则表达式语法
- **规则管理**: 支持保存、编辑和删除过滤规则

### 4. 波形可视化

#### 4.1 曲线窗口
- **多曲线支持**: 同时显示多条数据曲线
- **数据源**: 支持正则过滤和协议解析的数据作为输入
- **数据格式**: 支持格式如 `x:0xHH y:0xHH z:0xHH` 的数据解析

#### 4.2 波形配置
- **轴配置**: 自定义 X 轴和 Y 轴显示内容
- **数据绑定**: 将特定数据段（如 CMD_ID=0x15 的 DATA）绑定到曲线
- **实时更新**: 波形数据实时更新显示

### 5. 多窗口显示系统

#### 5.1 窗口类型
- **原始数据窗口**: 显示未经处理的串口数据
- **协议解析窗口**: 显示解析后的协议数据
- **正则过滤窗口**: 显示经过正则表达式过滤的数据
- **波形显示窗口**: 显示数据波形图表

#### 5.2 窗口管理
- **同步显示**: 多个窗口可同时显示不同处理阶段的数据
- **窗口布局**: 支持自定义窗口排列和布局
- **数据关联**: 窗口间数据保持关联和同步

### 6. 配置管理

#### 6.1 配置保存
- **配置导出**: 支持将用户配置导出为文件
- **配置导入**: 支持从文件导入配置到其他电脑
- **配置同步**: 跨设备配置同步功能

#### 6.2 配置内容
- **协议配置**: 自定义协议结构和解析规则
- **过滤规则**: 正则表达式过滤规则
- **窗口布局**: 多窗口的排列和显示设置
- **波形配置**: 曲线显示和数据绑定设置

### 7. 缓冲区管理

#### 7.1 内存优化
- **文件缓存**: 使用文件缓存避免过度占用内存
- **缓存大小**: 默认单文件缓存大小 1MB
- **自动清理**: 缓存文件满后自动清空

#### 7.2 数据存储选项
- **标准模式**: 使用固定大小缓存文件
- **完整模式**: 用户可选择保存全部数据
- **分类存储**: 不同类型数据（波形、原始数据、过滤数据）分别存储
- **无限制存储**: 用户创建的缓存文件不限制大小

### 8. 通用串口功能

#### 8.1 基础串口设置
- **波特率配置**: 支持标准波特率设置
- **数据位配置**: 5、6、7、8 位数据位
- **停止位配置**: 1、1.5、2 位停止位
- **校验位配置**: 无校验、奇校验、偶校验、标记校验、空格校验

#### 8.2 高级功能
- **流量控制**: 硬件流控制（RTS/CTS）、软件流控制（XON/XOFF）
- **数据统计**: 实时显示发送和接收的数据量
- **驱动选择**: 支持选择不同的硬件驱动
- **连接管理**: 串口的打开、关闭和重连功能
- **智能分包**: 自动处理大数据包的分包和重组

## 技术要求

### 性能要求
- **实时性**: 协议解析延迟 < 10ms
- **数据吞吐**: 支持最高 921600bps 波特率
- **内存占用**: 运行时内存占用 < 100MB
- **CPU 占用**: 正常使用时 CPU 占用 < 5%

### 兼容性要求
- **操作系统**: Windows 7+、macOS 10.12+、Linux
- **串口驱动**: CH340、CP2102、FTDI 等主流驱动
- **硬件架构**: x86、x64、ARM

### 可靠性要求
- **稳定性**: 7×24 小时连续运行
- **错误恢复**: 串口断开后自动重连
- **数据完整性**: 保证数据传输的完整性
- **异常处理**: 完善的异常处理和错误提示

## 用户界面要求

### 界面设计
- **直观性**: 界面布局清晰，功能分区明确
- **易用性**: 操作简单，学习成本低
- **可定制性**: 支持主题和布局的自定义
- **响应式**: 适配不同屏幕分辨率

### 交互设计
- **快捷键**: 支持常用操作的快捷键
- **右键菜单**: 丰富的上下文菜单功能
- **拖拽操作**: 支持文件和配置的拖拽导入
- **状态提示**: 实时显示连接状态和数据统计

## 开发优先级

### 第一阶段（核心功能）
1. 基础串口通信功能
2. 简单协议解析（固定格式）
3. 原始数据显示窗口
4. 基础配置管理

### 第二阶段（高级功能）
1. 自定义协议解析
2. 正则表达式过滤
3. 多窗口显示系统
4. 波形可视化

### 第三阶段（优化完善）
1. 性能优化和稳定性提升
2. 高级后处理功能
3. 完整的配置管理系统
4. 缓冲区管理优化

## 验收标准

### 功能验收
- 所有核心功能正常工作
- 支持指定的串口驱动器
- 协议解析准确率 99.9%
- 数据过滤和显示正确

### 性能验收
- 满足性能要求指标
- 压力测试通过
- 内存泄漏检测通过
- 长时间稳定性测试通过

### 用户体验验收
- 界面友好，操作流畅
- 文档完整，易于上手
- 错误提示清晰明确
- 用户反馈积极正面